#include <stdio.h>

int main()
{
    char specialization[20];
    printf("Enter the specilization : ");
    scanf("%s", specialization);
    printf("So the specilization is %s", specialization);
    return 0;
}