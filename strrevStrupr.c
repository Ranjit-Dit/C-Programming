// enter there name reverse it and convert into uppercase character using character arrays and their functions

#include <stdio.h>
#include <string.h>

int main()
{
    char name[30];
    printf("Enter the name : ");
    gets(name);
    printf("%s", name);
    return 0;
}